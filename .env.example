# ANTHROPIC_API_KEY is now managed via HashiCorp Vault (localhost:8200).
# You can still set it here as a fallback — Vault takes priority.
# ANTHROPIC_API_KEY=sk-ant-your-key-here
REDIS_URL=redis://redis:6379
RAW_DIR=/data/raw
FEATURES_DIR=/data/features
